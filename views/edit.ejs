<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-12">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">LogOut</a>
        </div>
        <h5 class="mb-5">Edit your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="What's on your mind"
                class="p-3 w-1/3 outline-none resize-none outline-none rounded-md bg-transparent border-2" name="content"
                ><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-500 block text-black rounded-md mt-2" type="submit" value="Update Post">
        </form>
</body>

</html>