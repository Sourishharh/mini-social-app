<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-12">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block" href="/logout">LogOut</a>
        </div>
        <h3 class="text-3xl mb-5">Hello, <%=user.name %>
        </h3>
        <h5 class="mb-5">You can create a new post</h5>
        <form method="post" action="/post">
            <textarea placeholder="What's on your mind"
                class="p-3 w-1/3 outline-none resize-none outline-none rounded-md bg-transparent border-2" name=""
                id=""></textarea>
            <input class="px-3 py-2 bg-blue-500 block rounded-md mt-2" type="submit" value="Create new Post">
        </form>
        <div class="posts mt-32">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 p-4 mb-4 rounded-md border-2 border-zinc-800">
                  <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                  <p><%= post.content %></p>
                  <small class="mt-3 inline-block"><%= post.likes.length %> Likes</small>
                    <div class="btns flex gap-4 mt-1">
                        <a class="text-blue-500" href="/like/<%=post._id %>">
                            <%= post.likes.indexOf(user._id) ===-1 ? "Like" : "unlike" %>
                        </a>
                        <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>  
            </div>
        <% }) %>
        </div>
    </div>
</body>

</html>